DATABASE_URL=postgresql://user:pass@host:5432/dbname?sslmode=require

# Fundly credentials
FUNDLY_EMAIL=you@example.com
FUNDLY_PASSWORD=your-password

# Preferred: Gmail API (set all four)
GMAIL_CLIENT_ID=
GMAIL_CLIENT_SECRET=
GMAIL_REDIRECT_URI=
GMAIL_REFRESH_TOKEN=
GMAIL_USER_EMAIL=

# SMTP fallback (set all four)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
FROM_EMAIL=
FROM_NAME=Fundly Bot

# Optional
EMAIL_SUBJECT=Funding Application — Next Steps

# Runtime controls
# Only the LaunchAgent should set ALLOW_EMAIL_SEND=true. Leave unset/false for manual runs.
ALLOW_EMAIL_SEND=false
# Keep this consistent with LaunchAgent StartInterval
SCAN_INTERVAL_SECONDS=15
# Optional alternate marker for LaunchAgent context
# RUN_CONTEXT=launchd
# When true, never send emails and never update send state (safe local testing)
DRY_RUN=false
